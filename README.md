# ⚔️ Influence - Telegram MiniApp Game

Стратегическая игра Influence с Telegram ботом и MiniApp интеграцией.

## 🎮 О игре

**Influence** — это пошаговая стратегическая игра, где игрок должен захватить как можно больше клеток территории противника.

### Правила:
- Игровое поле 5×5 с клетками разной силы (1-3)
- Твоя база начинается в левом верхнем углу
- Противник начинает в правом нижнем углу
- Каждый ход ты выбираешь клетку для захвата
- Победа достаётся тому, у кого больше клеток в конце игры

## 🚀 Требования

- Node.js 16+ 
- npm или yarn
- Telegram Bot Token (получи у [@BotFather](https://t.me/botfather))

## 📦 Установка

### 1. Клонирование и зависимости

```bash
# Установить корневые зависимости
npm install

# Установить зависимости клиента
cd client && npm install && cd ..
```

### 2. Настройка переменных окружения

Создай файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Отредактируй `.env`:

```env
# Telegram Bot Token (получи от @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# URLs
WEBHOOK_URL=http://localhost:3000
MINIAPP_URL=http://localhost:5173

# Server
PORT=3000
NODE_ENV=development
```

## 🎯 Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный token в `.env` файл

## 🏃 Запуск проекта

### Способ 1: Раздельный запуск (для разработки)

**Терминал 1 - Сервер Telegram бота:**
```bash
npm run dev
```

**Терминал 2 - React приложение:**
```bash
npm run client:dev
```

### Способ 2: Production build

```bash
# Собрать React приложение
npm run client:build

# Запустить сервер
npm start
```

## 📁 Структура проекта

```
.
├── server/
│   ├── index.js              # Главный сервер Express
│   ├── commands/
│   │   └── start.js          # Команда /start для Telegram
│   ├── database.js           # Инициализация БД и функции
│   └── gameHandler.js        # Логика игры и AI
├── client/
│   ├── src/
│   │   ├── components/       # React компоненты
│   │   │   ├── MainMenu.jsx
│   │   │   ├── GameBoard.jsx
│   │   │   ├── GameGrid.jsx
│   │   │   ├── GameStats.jsx
│   │   │   └── RulesScreen.jsx
│   │   ├── styles/          # CSS файлы
│   │   ├── App.jsx
│   │   └── main.jsx
│   ├── index.html
│   └── vite.config.js
├── data/                    # БД (создаётся автоматически)
├── package.json
└── .env                     # Переменные окружения
```

## 🔌 API Endpoints

### GET `/api/game/:userId`
Получить текущее состояние игры игрока

**Response:**
```json
{
  "userId": 123456789,
  "board": [[...], [...], ...],
  "playerScore": 5,
  "opponentScore": 4,
  "status": "active"
}
```

### POST `/api/game/:userId/move`
Отправить ход в игре

**Body:**
```json
{
  "action": {
    "x": 1,
    "y": 2
  }
}
```

### GET `/health`
Проверка здоровья сервера

## 🎮 Использование Telegram бота

1. Найди своего бота в Telegram (имя которого указал при создании)
2. Отправь команду `/start`
3. Нажми на кнопку "🎯 Играть в Influence"
4. Откроется MiniApp с игрой

## 💾 База данных

Проект использует SQLite для сохранения статистики игроков:

- **users** - информация об игроках
- **game_stats** - статистика побед/поражений
- **game_history** - история всех игр

БД файл автоматически создаётся в `data/game.db`

## 🤖 Игровая AI

Противник использует простую AI:
- Выбирает случайную доступную клетку для захвата
- Приоритизирует слабые клетки
- Может быть улучшена для более сложной стратегии

## 🛠️ Разработка

### Добавление новых команд

В файле `server/commands/` создайте новый файл:

```javascript
// server/commands/mycommand.js
export async function handleMyCommand(ctx) {
  await ctx.reply('Ответ на команду');
}
```

Затем добавьте в `server/index.js`:
```javascript
import { handleMyCommand } from './commands/mycommand.js';
bot.command('mycommand', handleMyCommand);
```

### Улучшение UI

Все компоненты находятся в `client/src/components/`
Стили в `client/src/styles/`

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте, что `TELEGRAM_BOT_TOKEN` правильно установлен в `.env`
- Убедитесь, что сервер запущен на порту 3000
- Проверьте логи консоли на ошибки

### MiniApp не открывается
- Убедитесь, что `MINIAPP_URL` правильно установлена
- React приложение должно быть запущено на порту 5173
- Проверьте консоль браузера на ошибки

### БД ошибки
- Убедитесь, что папка `data/` создана или будет создана автоматически
- SQLite файл должен быть доступен для записи

## 📚 Используемые технологии

- **Backend:** Node.js, Express, Telegraf
- **Frontend:** React 18, Vite
- **Database:** SQLite3
- **Styling:** CSS3
- **Telegram:** Web App API, Telegram Bot API

## 📝 Лицензия

MIT License - используй свободно!

## 🤝 Контрибьютинг

Предложения и улучшения приветствуются!

## 📞 Поддержка

Если у тебя есть вопросы или ошибки - создай Issue в репозитории.

---

**Приятной игры! 🎮⚔️**
